# Configurações do Neural Web Tester

# Token de autenticação para as requisições (Bearer JWT)
AGENT_TOKEN=seu_token_aqui

# Configuração do navegador Playwright (true para rodar sem interface, false para ver o navegador)
HEADLESS=true
